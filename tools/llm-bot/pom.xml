<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.finos.springbot</groupId>
		<artifactId>spring-bot</artifactId>
		<version>10.0.2</version>
		<relativePath>../../pom.xml</relativePath>
	</parent>

	<groupId>org.finos.springbot.tools</groupId>
	<artifactId>LLM-bot</artifactId>
	<name>LLM Bot</name>
	<description>Provides an instruction-based chat with an LLM</description>

	<properties>
		<maven.deploy.skip>true</maven.deploy.skip>
		<spring-boot.run.main-class>org.finos.springbot.tool.llm.LLMBotApp</spring-boot.run.main-class>
		<jaxb.version>2.3.1</jaxb.version>
		<jaxb-core.version>2.3.0.1</jaxb-core.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>ai.koog</groupId>
			<artifactId>koog-agents-jvm</artifactId>
			<version>0.3.0</version>
		</dependency>	
		<dependency>
			<groupId>org.finos.springbot</groupId>
			<artifactId>symphony-bdk-chat-workflow-spring-boot-starter</artifactId>
			<version>10.0.2</version>
		</dependency> 
		<!-- <dependency>
			<groupId>org.finos.springbot</groupId>
			<artifactId>teams-chat-workflow-spring-boot-starter</artifactId>
			<version>10.0.2</version>
		</dependency> -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
		</dependency>

		<!-- Add Jackson dependencies -->
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
		</dependency>

		<!-- Add logging dependencies -->
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
		</dependency>

		<dependency>
			<groupId>org.finos.springbot</groupId>
			<artifactId>chat-workflow-testing</artifactId>
			<version>10.0.2</version>
			<scope>test</scope>
		</dependency>

    </dependencies>
    
    

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<version>${spring-boot.version}</version>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
						</goals>
						<configuration>
							<mainClass>${spring-boot.run.main-class}</mainClass>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
